
import { Metadata } from "next";

const TITLE = "Setores";

export const metadata: Metadata = {
  title: TITLE,
};

const SectorsPage = () => {
  return (
    <>
      </>
  );
};

export default SectorsPage;
