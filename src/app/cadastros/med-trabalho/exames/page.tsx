
import { Metadata } from "next";

const TITLE = "Exames";

export const metadata: Metadata = {
  title: TITLE,
};

const OMExamsPage = () => {
  return (
    <>
      </>
  );
};

export default OMExamsPage;
