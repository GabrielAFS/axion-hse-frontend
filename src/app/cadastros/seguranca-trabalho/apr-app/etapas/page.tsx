
import { Metadata } from "next";

const TITLE = "Etapas";

export const metadata: Metadata = {
  title: TITLE,
};

const OSStepsPage = () => {
  return (
    <>
      </>
  );
};

export default OSStepsPage;
