
import { Metadata } from "next";

const TITLE = "Unidades";

export const metadata: Metadata = {
  title: TITLE,
};

const UnitsPage = () => {
  return (
    <>
      </>
  );
};

export default UnitsPage;
